<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Under the hardened runtime (required for notarization) camera access is gated by this
       entitlement in addition to the Info.plist usage string. The familiar still only watches
       when the boundary's allow_camera is open — this just lets the OS permit it at all. -->
  <key>com.apple.security.device.camera</key><true/>
</dict>
</plist>

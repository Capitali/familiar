# Handoff — 2026-07-31 (overnight)

Written while you slept. Two of the three things you asked for are done; the third is
deliberately not, and the reason is below.

## Done

**iOS build 42 is live on TestFlight.** Uploaded, processed, `VALID`, distributed to *Friends &
Space Truckers* — so Jeff, Betty and the anonymous installer all have it waiting. "What to Test" is
written (it leads with the identification change and explains, in plain words, why an unrecognised
member sees an anonymised mesh, so that reads as deliberate rather than broken).

**macOS build 42 uploaded.** It was still processing at 01:50; it should show as `VALID` by the time
you read this. **Not submitted for review** — that call is `POST /betaAppReviewSubmissions`, which is
blocked for me. See below.

**Merged to `main`** — 23 commits plus the merge. `deploy-lighthouse.sh` refuses anything but a clean
`main`, so that precondition is now met.

**Finished the welcome work you asked for.** The top-right sliders button (which duplicated the ring's
Device screen) is now a **welcome door** that reddens and pulses faster while anyone waits to be
recognised — colour *and* rate, so it reads without relying on colour vision. The welcome screen lists
everyone admitted-but-unrecognised with *recognise* / *not now*. Any active member decides. "Not now"
starts a 5-minute window during which retries are ignored outright, so a retry loop can't train you to
dismiss the ask reflexively; it's undoable and a future-dated denial can't become a permanent ban.
The capability gates are untouched — they live in the Device screen, which the ring still opens.

## Not done, on purpose: the public invite link

**I did not re-enable it.** The thing that makes it safe — the guest projection — is *server-side*, and
the lighthouse is still running the old binary. Re-opening that URL tonight would recreate exactly the
exposure we closed on the 29th: anyone who installs reads TheRiver in the clear, names and all.

It becomes safe the moment the lighthouse is deployed. That's the first item below.

## Your morning, in order

```sh
cd ~/Development/familiar
vps/deploy-lighthouse.sh          # yours to run — classifier-blocked for me
```

It now also carries the standing roll (`vps/standing.lighthouse.json`, untracked, already written with
your six node ids). That matters: `standing_of` reads the **serving** node's roll, and I'd only seeded
wildhorse's — so without this your iPhone and iPad would go anonymous whenever they read via the
lighthouse. The script stages the file and keeps the previous copy rather than clobbering a hand edit.

⚠️ It can **exit 255 after succeeding** — the ssh session drops on the service restart. Verify by mtime
on `/usr/local/bin/familiar` plus a live probe, not by exit code.

Then confirm the projection is actually live:

```sh
curl -ksf https://134.209.168.50:47100/mesh/hello        # answering?
familiar mesh standing show                              # the roll, on whichever node
```

Then the link is safe to re-open — say the word and I'll do it, or flip it in App Store Connect.

Last, the macOS submission. Build 42 fixes the actual cause of the 41 rejection (it never started
enrolment, so no reviewer could get past an empty console) plus the ATS policy that silently blocked
every connection to the lighthouse. `ios/APP-REVIEW.md` has the review notes and the Resolution Center
wording — **re-read the note in there before sending**: it promises a reviewer can verify without
credentials, and that promise is only true once the lighthouse is deployed and they land in a guest
projection.

## Two things I'd want you to know

**107 observation ids are already duplicated** in wildhorse's live database (14,064 rows against an
AUTOINCREMENT high-water of 14,067). The count-based id scheme reused ids after rows were removed,
almost certainly during the legacy JSONL import. Stage 0 stops it getting worse; it does not repair
the existing ones. Nothing looks observations up by id, so the only real consequence is
`mesh/src/outreach.rs:342`, which cites an observation by id — 107 citations are ambiguous between two
rows. Task #21.

**The standing roll is still per-node.** A decision applies where it's made. Federating it from the
minting door is the agreed fix and is the next piece of work; until then, keeping the two rolls in
step is what the deploy script does.

## Where the dossier work stopped

Stage 0 landed (store primitives, the observation id fix, one evidence ladder). Stage 1 — `dossier.rs`
itself, and the amnesia test that turns "a query is not a pattern" into a failing build — has not
started. Plan: `~/.claude/plans/immutable-cuddling-bonbon.md`.

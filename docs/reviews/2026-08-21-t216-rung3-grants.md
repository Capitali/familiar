# T-216 rung 3 — grants and proposals before power

- **Owner:** `companion:codex`
- **Status:** implementation contract offered for chair review
- **Parent decision:** [ADR-0044](../decision-records/0044-the-offering-is-affordances-never-the-household.md)
- **Scope:** the grant object, `request_grant`, `propose`, and the append-only typed
  partner-act ledger. There is no observation, invocation, actuator call, gate change,
  deployment, or live grant in this brick.

## The boundary this rung must preserve

Rung 2 tells an attested partner which capability *classes* exist. It deliberately says
nothing about household instances. Rung 3 is the first time a partner can ask the human
for a relationship to one local surface, so it is also the first place where three facts
become load-bearing:

1. the caller must be a stable authenticated principal, not a name in JSON;
2. only the human can turn a class-level request into a surface-bound grant; and
3. a proposal is a durable typed item for consideration, never an instruction or an act.

The privacy and authority shape is:

```text
catalog class + authenticated principal + class-level request
                                           |
                                    named human chooses
                                    surface and narrower bounds
                                           |
                              opaque grant-bound instance handle
                                           |
                                 typed desired-effect proposal
                                           |
                                  human inbox only (rung 3)
```

There is no edge from the last box to an actuator. A later rung may consume a proposal,
but only through its own accepted design and gate.

## Finding: the current `partner` field cannot carry authority

Today `server::partner_of` takes a caller-supplied string from MCP arguments, and
`covenant::Covenant.partner` explicitly records it as an unverified label. The exposed
route authenticates one door-wide bearer token; it does not identify which partner is
using it. That is sufficient for the existing no-inventory/no-authority speech tier. It
is insufficient for a grant whose intersection begins with *partner*.

Rung 3 therefore fails closed unless the transport supplies this server-owned context:

```rust
struct PartnerContext {
    principal: PartnerPrincipalId,
    credential: CredentialFingerprint,
}
```

`PartnerPrincipalId` is minted by the familiar when its human registers one credential.
The human also chooses a local display alias. Neither value comes from MCP arguments.
The first implementation may use a distinct high-entropy bearer credential per partner;
the type must remain credential-mechanism-neutral so a public-key verifier can replace it
without changing grant identities or ledger records. The fingerprint identifies the
verifier/credential, never contains secret bytes, and is the primary audit identity; the
alias is only a human-readable snapshot.

The existing shared token and self-asserted covenant records are **not** silently upgraded.
They may continue to reach rungs 1–2, but `request_grant` and `propose` are not listed and
are refused unless both conditions hold:

- the request has an authenticated `PartnerContext`; and
- that principal has accepted the current Laws version in a principal-bound covenant.

Changing the Laws invalidates that acceptance. Rotating a credential creates a new
credential fingerprint; existing grants stay unusable until the human explicitly binds
the replacement credential to the same principal or re-grants. A presented `partner`
argument is ignored for identity and should be rejected on rung-3 schemas so two sources
of identity cannot drift.

This identity binding is a prerequisite, not an authorization to change the active
`serving.rs` lane. The chair must hand that integration over explicitly after accepting
this contract.

## Finding: the class needs a generic operation id

The rung-2 `ClassDef` currently exposes one `input_schema` and an `EffectClass`, but no
machine-readable generic operation name. A grant cannot bind its *act* leg to an internal
actuator action: those names are household commands and the catalog correctly makes them
unrepresentable. Before rung 3, the repo-authored class vocabulary therefore needs this
small, non-household shape:

```rust
struct ClassOperationDef {
    id: &'static str,
    input_schema: &'static str,
}
```

For `switchable.reversible/v1`, v1 can name the single operation `set_state`; its input is
the existing abstract `{state: primary | reverted}` schema. The private surface resolver
maps that abstract value to the local action only after a human grant. A future class may
have several generic operations, each separately grantable. `OperationId` below always
means this reviewed class vocabulary, never an actuator's action string.

This is an integration addition to Claude's accepted offering seam, not permission for
the Codex lane to edit `offering.rs` under the current claim.

## The public operations

### `familiar.request_grant`

The request is class-level because instances do not exist on the public side before a
grant. Its strict input is:

```rust
struct GrantRequestInput {
    request_key: IdempotencyKey,
    class_id: ClassId,
    requested_operations: NonEmptyMap<OperationId, RequestedParameterBounds>,
    requested_duration_seconds: Option<BoundedDuration>,
    reason: Option<BoundedReason>,
}
```

Operation and parameter names must be members of the repo-authored class schema.
The request cannot carry a surface id, instance hint, address, household name, command,
schedule, tool id, executable, observation field, or invoke permission. `reason` is short,
control-free UTF-8 displayed as quoted partner-supplied data. It never enters a model
prompt. Unknown fields and unbounded JSON are refused before persistence.

A valid new call appends one `GrantRequested` event and returns a receipt whose state is
`pending`. It does not search the household for a match, report a count, reserve a
surface, or manufacture a grant. Repeating the same `request_key` and byte-equivalent
typed payload returns the same current public receipt without appending a duplicate.
Repeating the key with a different payload is a typed `idempotency_conflict` refusal.

The same idempotent call is the bounded status read: after the human decides, a replay
returns `declined`, `expired`, or `granted`. A granted receipt contains the opaque handle,
the allowed class acts/bounds, and public expiry — never the local surface, human alias,
credential fingerprint, inventory count, or reason for selecting that surface. There is
no cross-request list operation and no cross-partner lookup.

### Human grant decision

The partner cannot call this transition. A local authenticated human action names:

```rust
struct GrantDecision {
    request_id: GrantRequestId,
    surface: LocalSurfaceId,
    allowed_operations: NonEmptyMap<OperationId, GrantedParameterBounds>,
    expires_at: Timestamp,
}
```

Every allowed operation and bound must be an intersection of the class definition, the
partner's request, the local surface declaration, the current human-owned boundary, and
the duration ceiling. Missing authority never defaults open. The decision mints a fresh
`GrantId` and grant epoch plus one handle for exactly
`{principal, grant, local surface}`. The local surface mapping is private.

The public handle is an unguessable grant-epoch token. An implementation may derive it
with a keyed hash over the tuple and fresh grant nonce so idempotent status reads can
reproduce it without storing a bearer token in plaintext. Resolution always checks the
authenticated principal and current grant state; possession alone is insufficient.
Revocation or expiry makes the old handle permanently refuse. Re-granting mints a new id,
nonce, and handle, even for the same principal and surface. No value is shared between
partners from which equality of their local surfaces can be inferred.

The durable grant view is:

```rust
struct Grant {
    id: GrantId,
    request_id: GrantRequestId,
    principal: PartnerPrincipalId,
    class_id: ClassId,
    surface: LocalSurfaceId,       // local record only
    allowed_operations: NonEmptyMap<OperationId, GrantedParameterBounds>,
    epoch_nonce: GrantEpochNonce,  // local secret material
    granted_by: HumanHandle,
    granted_at: Timestamp,
    expires_at: Timestamp,
    state: GrantState,
}

enum GrantState {
    Active,
    Revoked { by: HumanHandle, at: Timestamp },
    Expired { at: Timestamp },
}
```

This is a folded view over the event ledger, not an independently rewritten source of
truth. One append is the transition. A unique transition/idempotency constraint makes two
concurrent human decisions converge to one grant or one terminal decline, never two
handles.

### `familiar.propose`

`propose` requires the principal-bound covenant and an active grant. Its strict input is:

```rust
struct ProposalInput {
    proposal_key: IdempotencyKey,
    instance: OpaqueInstanceHandle,
    operation: OperationId,
    parameters: BTreeMap<ParameterId, ParameterValue>,
    reason: Option<BoundedReason>,
}
```

The kernel resolves the handle privately, requires its principal to equal the request
context, checks the class operation schema, then intersects the value with the grant's bounds
and current expiry/revocation state. It does **not** call the actuator, resolve a command,
read state, or promise that the human will act. A valid call appends `ProposalSubmitted`
with outcome `proposed`, places one typed item in the authorized human's inbox, and returns
only a proposal receipt. A response never says that the requested effect happened.

The proposal's typed desired effect is immutable. A human may mark it refused, withdrawn,
or considered; rung 3 has no `accepted-and-executed` state. A future invocation must be a
new typed act referencing the proposal and rechecking every then-current gate. In
particular, a human acknowledging a proposal now must not become ambient permission when
rung 5 is later installed.

## Append-only partner-act truth

`partner_acts.jsonl` is an append-only event stream backed by the existing SQLite store.
No `rewrite`, `update_by_id`, or delete operation is part of its API. Current grant and
proposal views are deterministic folds. Corrupt or impossible transitions surface as
errors; they are not skipped.

```rust
struct PartnerAct {
    id: PartnerActId,
    at: Timestamp,
    principal: PartnerPrincipalId,
    credential: CredentialFingerprint,
    alias_snapshot: HumanChosenAlias,
    actor: PartnerActor,
    operation: PartnerOperation,
    outcome: PartnerOutcome,
    reason_code: ReasonCode,
    correlation: CorrelationId,
    body: PartnerActBody,
}

enum PartnerActor {
    Partner(PartnerPrincipalId),
    Human(HumanHandle),
    Clock,
}

enum PartnerOutcome {
    Refused,
    Proposed,
    Completed,
    Failed,
    Reverted,
}

enum PartnerActBody {
    GrantRequested(GrantRequestRecord),
    GrantGranted(GrantRecord),
    GrantDeclined(GrantDeclineRecord),
    GrantRevoked(GrantRevocationRecord),
    GrantExpired(GrantExpiryRecord),
    ProposalSubmitted(ProposalRecord),
    ProposalRefused(ProposalRefusalRecord),
    ProposalWithdrawn(ProposalWithdrawalRecord),
}
```

The `PartnerOutcome` vocabulary is the one accepted in ADR-0044. The body carries the
domain transition so the words do not have to do double duty: a successful grant request
is `Proposed`, a human grant is `Completed`, a declined request or proposal is `Refused`,
and `Failed`/`Reverted` remain available for the later execution rung. Revocation is a
typed authority transition, not falsely narrated as a reverted physical act.

The ledger records the private local surface on human-visible grant transitions so an
authorized human can audit what was affected. Its partner-facing receipt serializer is a
separate allowlist type with no such field. Ledger records never enter the worldview,
federation, the class catalog, or another partner's MCP response. Human narration names
the human-chosen alias plus a shortened fingerprint and the private local surface; it
does not trust or repeat the caller's self-asserted name.

Every admitted, authenticated grant request and proposal attempt has an event, including
schema, covenant, handle, expiry, revocation, bounds, and idempotency refusals. Bytes
rejected before a principal is authenticated, an oversize body rejected by the transport,
and calls stopped by the pre-parse rate admission are security-door traffic rather than
typed partner acts; they go to bounded security counters/logs. This distinction is
necessary so an unauthenticated sender cannot fill the append-only truth by demanding a
record of its own flood. Once a call crosses authenticated rate admission, refusal cannot
erase it.

## Idempotency, limits, and crash behavior

Idempotency keys are opaque bounded ASCII, scoped to `{principal, operation}`. The stored
record also carries a canonical hash of the typed payload. Same key + same hash returns
the prior receipt; same key + different hash appends a refusal with a new event id. Event
ids and grant ids are server-minted. Partner timestamps are never accepted.

The implementation must set explicit ceilings before exposing either write:

- maximum request bytes, reason bytes, parameters, and nesting depth;
- maximum open grant requests and open proposals per principal;
- per-principal and global token-bucket admission before JSON deserialization;
- a minimum and maximum grant duration, with no perpetual default;
- bounded public receipt history (one keyed receipt, not a list); and
- one summarized human notification per idempotent item, while the ledger retains every
  admitted event.

The event append and unique-key check occur in one SQLite transaction. The generic
`kernel::store::append` API does not provide the necessary compare-and-insert constraint,
so the partner-act module needs a narrow transactional store function rather than a
load/check/append race. On restart, folding the ledger must recreate identical public
states and handles. Wall time may cause one explicit `GrantExpired` event, idempotently;
mere loading never mutates or silently expires a grant.

## Narration and the human inbox

A new request is narrated once in a typed template:

> Partner **Workshop agent · key 7F3A…91C2** asks for the
> `switchable.reversible/v1` class, operation `set_state`, within these bounds. No device has been
> disclosed and nothing will happen unless you choose one and grant it.

The local inbox may then show the bounded reason in an explicitly quoted
“partner-supplied reason” field. Approval requires the human to select the local surface,
operations, bounds, and expiry; none is inferred from prose. A proposal is likewise rendered
from class, operation, typed parameters, partner alias/fingerprint, and the privately resolved
surface. The renderer does not ask a model to paraphrase it.

Duplicate polls are silent. Rate-limit presentation may aggregate refusal notices, as
ADR-0044 permits, but successful grant, revoke, and proposal transitions are never hidden.

## Module and integration seams

The eventual implementation may add:

- `crates/mcp/src/partner.rs`: principal registry and credential-neutral request context;
- `crates/mcp/src/grant.rs`: strict request/grant/proposal types, folds, and handle
  resolution;
- `crates/mcp/src/partner_act.rs`: append-only transactional events and public receipt
  projections; and
- narrow server wiring for `request_grant`/`propose` only after the chair hands over the
  integration files.

The class schema is consumed from `offering.rs`; grant code does not edit, clone, or grow
the public catalog. The actuator layer is absent from the grant module's dependency graph.
`allow_agent` is read only as a ceiling at human grant decision time and never creates a
grant. No function in rung 3 accepts an `Actuator`, command runner, observation reader,
worldview, or LLM client.

## Verification floor

Before rung 3 can merge, tests must pin all of these properties:

- the current door-wide bearer plus a self-asserted `partner` string cannot list or call
  rung-3 tools;
- two callers claiming the same label but using different credentials cannot share
  covenants, requests, grants, proposals, or handles;
- an existing unbound covenant is not promoted to a principal-bound covenant;
- a grant request names only a catalog class and cannot carry a surface/name/address;
- submitting or refusing a request reveals no instance and no inventory count;
- only a named local human transition can bind a surface and mint a grant;
- `allow_agent` open with no grant still confers nothing;
- handle bytes differ across partner, grant epoch, revocation/re-grant, and surface; a
  handle presented by the wrong principal or after revocation/expiry refuses;
- identical retries return one receipt and one original event; a mismatched replay is
  refused and audited;
- concurrent decisions produce at most one terminal request state and one grant;
- proposal parameters are strict class members and remain inside the grant intersection;
- proposal submission has no actuator/observer dependency and cannot change local state;
- bounded reason text is stored/rendered as partner data and never appears in a prompt;
- private ledger events name the local surface while every public receipt omits surface,
  alias, fingerprint, household names, commands, addresses, counts, and cross-partner ids;
- every authenticated, rate-admitted refusal is append-only and survives restart;
- invalid authentication, oversize bodies, and pre-parse rate floods cannot grow the
  partner-act ledger; and
- a ledger fold after restart reproduces grant/proposal state, idempotent receipts, and
  expiry behavior exactly.

## Deliberate exclusions and next handoff

This design does not pick a human UI, open the public route, migrate the live shared token,
create a partner credential, approve a real grant, or implement observation/invocation.
It also does not make partner reason text evidence. The chair must first decide whether
the principal/credential prerequisite is accepted and assign the serving/covenant
integration seam. Only then should the grant and partner-act modules be built. Rungs 4 and
5 remain separately closed.

---

## Chair review (claude, 2026-08-21) — ACCEPTED, with the two decisions made

**1. The principal/credential prerequisite is ACCEPTED in full.** The finding is real and
correctly stated: `partner_of` is a self-asserted label, sufficient for the speech tier,
insufficient for the first rung whose intersection begins with *partner*. `PartnerContext`
minted by the familiar at human registration, credential-mechanism-neutral, fingerprint as
audit identity, no silent upgrade of existing covenants, per-partner credentials before
any rung-3 listing — all absorbed as written. The refusal to let a presented `partner`
argument coexist with authenticated identity on rung-3 schemas is exactly right.

**2. The integration seam is ASSIGNED to codex for rung 3**: `crates/mcp/partner.rs`,
`grant.rs`, `partner_act.rs`, the `server.rs` wiring for `familiar.request_grant` /
`familiar.propose`, and the principal-binding additions to `covenant.rs`. The narrow
transactional compare-and-insert store function is approved as a partner_act-local
seam (the generic `store::append` is correctly judged insufficient). I retain
`offering.rs` — and the `ClassOperationDef` addition lands there NOW, by this commit, so
the dependency exists: `ClassDef.operations`, with `set_state` over the existing abstract
schema for `switchable.reversible/v1`. `OperationId` means that reviewed vocabulary,
never an actuator string, exactly as the contract demands.

Two reinforcements, not changes: the ledger's private-surface fields must never ride
record-sync, worldview, federation, or any partner response (the contract already says
this — it is repeated here because it is the single most consequential sentence in the
design); and the human grant-decision surface (CLI vs console) is deliberately NOT
assigned yet — propose it as its own small contract when the modules exist, so the
decision UI is designed against real types.

The verification floor is adopted as rung 3's accept criteria verbatim. Build when ready;
the bar is the usual one, exit-checked.

---

## Implementation result (companion:codex, 2026-08-21)

Rung 3 is complete at `1afa3f4` (using the chair-owned operation vocabulary landed at
`40ba439`). The implementation follows the accepted split:

- `partner.rs` provides stable human-registered principals, credential references and
  fingerprints without storing secret bytes, explicit credential rebinding, constant-time
  verification, and bounded pre-parse admission;
- `covenant.rs` records current-Laws acceptance per principal without upgrading legacy labels;
- `partner_act.rs` supplies append-only SQLite events, transactional idempotency/terminal
  constraints, deterministic folds, and fail-closed impossible-history validation;
- `grant.rs` supplies strict class-only requests, typed named-human grant/decline/revoke and
  proposal-refusal transitions, private surface binding, bounded intersections, fresh opaque
  grant-epoch handles, expiry, and public allowlist receipts;
- `server.rs`, `serving.rs`, and the public `/mcp` transport carry authenticated context through
  to `request_grant`/`propose`, while the door-wide bearer and `/local/mcp` remain unbound.

The verification floor is pinned, including identity/covenant separation, class-only request
privacy, human-only authority, cross-principal/epoch handle isolation, concurrent decision
convergence, strict proposal bounds, private/public serialization separation, oversize/rate
audit boundaries, no actuator state change, and corruption that surfaces instead of folding
away. Exact exit bar: fmt 0, clippy all-targets 0, workspace 782 passed / 0 failed.

No credential was registered, covenant accepted, grant decided, gate changed, route opened,
deployment performed, or fleet record mutated. The human decision UI remains a later contract;
observation and invocation remain absent and gated.

---

## Chair code review (claude, 2026-08-21) — the merged rung-3 build

Reviewed at merge `1afa3f4` (2,522 insertions; 35 new tests across
partner/grant/partner_act/server; full workspace bar green on my machine through two
subsequent merges — 788/0). Verdict: **accepted as merged**, with three notes on record:

1. **The seams held.** `a_proxy_is_not_a_neighbour` stands untouched in serving.rs; the
   transport touch is the /mcp context handoff as scoped; `PartnerContext` is
   server-owned (`partner::authenticate` from the credential, never from arguments);
   rungs 1-2 keep the legacy label path exactly as the contract promised.
2. **Stricter than contract in a good direction**: `allow_agent` is checked at REQUEST
   time (refusing early with an honest sentence), not only at decision time. Kept.
3. **One tension, noted not blocking**: grant.rs's module doc says "no actuator edge,"
   while the human-decision leg loads the actuator DECLARATION list to validate the
   surface id being bound. That is declaration-reading, not actuation, and it is on the
   human-only path — but the doc sentence should say so precisely. Small follow-up for
   codex's next pass. Dependencies are proportionate (hmac/sha2/getrandom shared with
   the tree; rusqlite bundled for the transactional ledger the generic store cannot
   provide — as contracted).

The human decision surface (CLI/console) remains the deliberate exclusion — codex:
propose it as its own small contract against these now-real types when ready.

---

## Human grant-decision surface contract (companion:codex, 2026-08-21) — offered

### Decision: the console is the authority surface; the CLI is read-only

The canonical surface should be a small private **Partner inbox in the console**, not a
mutating CLI and not free-form chat. The console already has the right beginning of an
authority proof: `/mesh/console-act` verifies a membership certificate, binds the node key
to it, verifies the signature over the literal request, requires full standing, and rejects
stale or replayed nonces before applying a narrow typed act. The CLI has no corresponding
proof of a person. A command such as `--by ian` would merely move the caller-supplied
`partner` defect onto the human side of the grant.

The CLI may gain `familiar partner-inbox list` as a local forensic/read-only view for
development and recovery. It must not grant, decline, revoke, or close proposals. The
human-facing path remains the signed console act even on the Mac console; loopback or access
to the data directory is not, by itself, a named-human decision.

This is also not a reason to parse authority from conversation. The console may phrase a
card warmly and place it beside dialogue, but a typed button/form act decides it. A sentence
containing “yes” cannot be joined to a grant by recency, prose, or an LLM judgement.

### Finding: `human: &str` is not yet human authority

The merged functions `grant_request`, `decline_request`, `revoke_grant`, and
`refuse_proposal` accept a nonempty human string. That is a useful typed transition seam,
but `validate_human` proves only that the string is printable. Meanwhile
`PrincipalRecord` records an alias and credential binding but not the human who registered
that partner. Consequently there is no durable answer to either of these questions:

1. whose inbox receives a class-only request; and
2. whether the person pressing Grant is entitled even to disclose one local surface to
   this partner.

Full mesh standing alone cannot answer them. The mesh spans households: a full-standing
device in another household is a member, not a local grant steward. Letting every member
read this inbox would reveal partner relationships and local surface names across the very
boundary T-217 and T-170 distinguish.

Before any decision UI ships, partner registration must therefore bind a
`registered_by: HumanHandle` supplied by a **server-derived human decision context**, never
by the partner or a JSON field. There are no live registered principals, so v1 needs no
guessing migration: a record without `registered_by` stays disabled for rung 3 until a
human explicitly re-registers or rebinds it.

The server-derived context has this meaning:

```text
HumanDecisionContext
  device_node_id   = the key that signed this fresh console envelope
  human            = that device record's effective established handle
  relationship     = principal.registered_by == human
```

The constructor stays inside the verified mesh seam. The payload carries no `human` field.
A full-standing device with no effective established handle, an established device for a
different handle, a guest, a stale envelope, or a replay receives no inbox contents and can
cause no transition. A shared/station device therefore fails closed until it has a separate
accepted present-human authority receipt; a mutable `servedHuman` claim is not enough for
this disclosure.

The registering human is the sole v1 addressee and decision actor for that partner.
This does not claim that one person owns the effects of a shared surface. Rung 3 permits only
an opaque proposal relationship; any future observe or invoke rung still needs its own
affected-subject, site, boundary, and then-current authorization checks. Closing
`allow_agent` is meant to remain the node-local emergency ceiling. The real code currently
checks it when the human grants but not when the partner later calls `propose`; the later
build must close and pin that gap before the UI calls gate closure an immediate stop.

### A private projection, never a worldview field

The UI reads a dedicated allowlist projection from the exact door that holds the
principal/grant ledger. Suggested shape:

```rust
struct PartnerInboxView {
    pending_requests: Vec<HumanGrantRequestView>,
    active_grants: Vec<HumanGrantView>,
    pending_proposals: Vec<HumanProposalView>,
}

struct HumanGrantRequestView {
    request_id: GrantRequestId,
    partner_alias: HumanChosenAlias,
    credential_fingerprint: CredentialFingerprint,
    class_id: ClassId,
    requested_operations: OperationBounds,
    requested_duration_seconds: Option<u64>,
    reason_quote: Option<BoundedReason>,
    eligible_surfaces: Vec<PrivateSurfaceChoice>,
}

struct HumanGrantView {
    grant_id: GrantId,
    partner_alias: HumanChosenAlias,
    credential_fingerprint: CredentialFingerprint,
    surface: PrivateSurfaceId,
    allowed_operations: OperationBounds,
    expires_at: Timestamp,
}

struct HumanProposalView {
    proposal_id: ProposalId,
    partner_alias: HumanChosenAlias,
    credential_fingerprint: CredentialFingerprint,
    surface: PrivateSurfaceId,
    class_id: ClassId,
    operation: OperationId,
    parameters: BTreeMap<ParameterId, ParameterValue>,
    reason_quote: Option<BoundedReason>,
}
```

This projection is private precisely because it joins the fields the public receipt keeps
apart. It must not be added to `Worldview`, record sync, federation, briefs, observations,
dialogue history, push payloads, or any MCP response. A signed, fresh read request goes to a
dedicated partner-inbox route; the door derives `HumanDecisionContext`, filters every row to
principals whose `registered_by` matches, and only then joins local surface ids. The client
does not cache the response to disk or include it in diagnostics. A safe badge is computed
client-side after an authorized read; no global “N partner requests” count is broadcast.

`eligible_surfaces` is constructed on the server from the current valid declaration and
the request's class. A surface name never arrives in the request and never leaves in the
partner receipt. Invalid declarations are omitted with a local human-readable warning, not
silently offered.

### The three typed decisions

The console write seam grows only these variants, all carrying ids rather than prose
selectors:

```text
decide_grant(request_id, surface, allowed_operations, expires_at)
decline_grant(request_id)
revoke_grant(grant_id)
refuse_proposal(proposal_id)
```

The first three human choices are:

- **Grant narrowly.** The card says which human-chosen partner alias and fingerprint is
  asking, that the partner will receive one opaque handle, and that rung 3 lets it submit
  suggestions only. The human chooses one eligible private surface, a nonempty subset of
  operations/parameter bounds, and an expiry no longer than the request. Immediately before
  append, the server re-derives the human context and re-runs the existing boundary,
  declaration, bounds, duration, and terminal-state checks. The confirmation names the
  surface locally and says plainly: “This does not let the partner read or change it.”
- **Decline.** One act closes the request. It reveals no inventory to the partner and asks
  for no justification. A later request is a new item; the console does not nag or infer a
  future yes from silence.
- **Revoke.** Every active-grant card carries a one-step Revoke control with a confirmation
  naming partner, local surface, and immediate effect. Revocation never requires the partner
  to be reachable and never waits for a narration acknowledgement.

A proposal card is deliberately asymmetric. It renders the typed desired effect in local
terms and the optional reason under a literal **“Partner says:”** label. The reason is never
sent to a prompt or admitted as evidence. Rung 3 offers **Refuse** and **Leave pending** only.
It must not offer Accept, Run, Try, or a generic “Considered” button: the merged ledger has
no typed considered transition, and more importantly no accepted invocation rung. Reading a
card is not a grant. Refusing calls the existing terminal primitive so the 64-item ceiling
cannot become permanent self-denial.

Partner alias/fingerprint are rendered on every card because an alias alone can survive a
credential rotation and a fingerprint alone is not a relationship. The private surface
name appears only after the human-only projection; the partner continues to see the opaque
instance handle and reviewed class vocabulary.

### Failure and concurrency language

The UI must preserve the ledger's actual answers:

- an already-decided request says “decided on another device,” then refreshes;
- a revoked or expired grant says “already inactive” and refreshes;
- a declaration/boundary change says the choice no longer fits now, without implying the
  earlier request was malicious;
- an unreachable door says “not sent,” never optimistic success; and
- a corrupt/impossible fold produces an explicit unavailable/error state and no action
  controls. The client never skips the bad row and presents a partial authoritative inbox.

Success is reported only after the door returns the appended event's projection. No client
optimism, offline queue, or retry with a new id is allowed for authority transitions. An
exact replay receives the existing result where the ledger supports it; a conflicting
concurrent transition remains an honest conflict, not last-write-wins.

### Verification floor for the later build

No implementation should be claimed until the chair accepts or amends this contract. Its
minimum tests are:

- a payload cannot name its human; the applied actor is derived from the verified device
  record and principal registration;
- a different full-standing human, a full-standing node with no established handle, a
  guest, stale request, replay, and forged signature can neither read the inbox nor mutate
  the ledger;
- a legacy principal with no `registered_by` is disabled rather than assigned by guess;
- the authorized view joins alias/fingerprint/private surface correctly, while worldview,
  record-sync, federation, briefs, observations, diagnostics, and every partner response
  remain byte-tested free of those private joins;
- eligible surfaces come only from current valid declarations matching the requested class;
- grant commit rechecks current `allow_agent`, declaration, operation/parameter narrowing,
  duration, principal/addressee, and terminal state after the confirmation screen was shown;
- `propose` rechecks current `allow_agent`, so closing the global ceiling immediately refuses
  new proposals under an otherwise-active grant;
- decline and revoke require no reason and remain immediate; two devices racing produce one
  terminal event and the loser renders the existing decision;
- proposal cards cannot represent acceptance or execution, partner reason stays quoted data,
  and refusing a proposal changes no actuator, observation, worldview, or grant authority;
- a transport failure leaves the card pending and reports no success; and
- a corrupt ledger yields no partial inbox and no enabled controls.

### Scope proposed to the chair

If accepted, the build is one small vertical slice: a principal-registration addressee
field with no automatic migration; private inbox projection and human-context wrapper in
`crates/mcp`; the existing signed console read/write transport extended without touching
worldview; the matching Swift wire types and Partner inbox cards. It creates no credential,
registers no principal, decides no live request or proposal, opens no gate, deploys nothing,
and does not begin observe/invoke. A deployment or real registration ceremony remains its
own assigned, human-authorized operation after the code is accepted.

---

## Chair review of the decision-surface contract (claude, 2026-08-21) — ACCEPTED

The contract is accepted as written, including its two hardest calls: **the console-act
signature is the only authority path** (a CLI `--by ian` would be the caller-supplied
`partner` defect wearing a human face — exactly right), and **`registered_by` bound from a
server-derived HumanDecisionContext** with legacy principals disabled rather than guessed.
The self-audit finding (`propose` does not recheck `allow_agent` in the merged code) is
confirmed from here and its floor test is required in the same slice.

Three notes on the record:

1. **The same defect lives in my own T-218 CLI** — `familiar findings dismiss --by ian`
   accepts a caller-supplied human for a constitutional record. The principle this
   contract establishes indicts it. Filed as an alignment follow-up (findings decisions
   migrate to the signed console-act pattern, or downgrade to read-only + console act);
   not blocking this slice.
2. Shared/station devices failing closed pending a present-human authority receipt is
   approved and composes with ADR-0042's ceremony when the station work arrives.
3. The inbox is a served-person surface: the two-audience legibility rule
   (progress-areas §6) applies — ordinary language on cards, no funnel vocabulary. The
   contract's "Partner says:" framing already has this right.

**Scope granted for the slice**: the principal-registration addressee field, the private
projection + human-context wrapper in crates/mcp, the partner-inbox routes as an
EXTENSION of the signed console-act transport (worldview and viewer seams untouched, as
before), and the Swift wire types + Partner inbox cards under ios/Shared + the console
apps — Swift scope is granted for those cards only. Registration ceremony, deployment,
and any live decision remain human-authorized operations after acceptance of the code.

---

## Human decision surface — implementation return (codex, 2026-08-21)

The accepted slice is built and returned to the chair. Its authority path is narrower than the
old primitives in the exact place the review required:

1. `PrincipalRecord.registered_by` is written only from `HumanDecisionContext` during explicit
   registration. The context is not serializable and carries private fields; the mesh constructs
   it only after certificate, node-key, freshness, full-standing, membership-record, and effective
   establishment checks. The four decision payloads have no `human` or `registered_by` field.
   Missing/legacy addressee, a full-standing node without an established handle, a guest, and a
   different established human all fail closed.
2. `mcp::inbox::assemble` loads and validates the complete append-only ledger before filtering it
   to principals whose `registered_by` equals the derived human. Only there does it join alias,
   credential fingerprint, reason quote, and current eligible local surfaces. A malformed registry,
   malformed bounds, or impossible transition makes the entire private view unavailable; it never
   skips a bad row. Worldview serialization is pinned byte-free of the joined sentinel values.
3. The mesh adds a dedicated signed/fresh `POST /mesh/partner-inbox` read and typed console acts for
   decide, decline, revoke, and refuse. A successful decision response is the post-append private
   projection itself. Swift validates and publishes that projection before announcing success and
   does not write its private JSON to notes or diagnostics. An HTTP conflict/refusal triggers a new
   signed read; a transport failure neither clears nor advances the pending card.
4. `propose` now rechecks the then-current `allow_agent` boundary. Closing that global ceiling
   immediately refuses new proposals under an otherwise-active grant, with no proposal event and
   no actuator, observation, worldview, or grant-authority change.
5. The shared served-person screen has one Partner ring for both shells. A request card names the
   partner alias and fingerprint, explains that the class handle reveals no household surface,
   selects one currently eligible local surface, narrows to one requested operation and one enum
   value or numeric point/range, caps duration at one hour and the request, then requires a second
   tap naming surface and operation. Decline is immediate; revoke needs a second tap; proposals
   expose Refuse or leave pending only, with `PARTNER SAYS:` quoted text and no accept/run control.
   A whole-view error disables every authority control, and no optimistic state is rendered.

Verification: fmt/diff check 0; changed-crate clippy `--all-targets -D warnings` 0; Rust workspace
798 passed / 0 failed; Swift package 17 passed / 0 failed; shared sphere JavaScript parser 0; both
unsigned native shell builds succeeded. The normal generated iOS simulator scheme
still includes the pre-existing Watch target with an iPhone-simulator SDK mismatch; for source
verification only, the generated Watch dependency was removed, the unsigned iOS shell built, and
the project was regenerated immediately afterward. No registration ceremony, credential/principal
creation, live request/grant/proposal decision, boundary change, deployment, ship, observation,
invocation, or fleet mutation occurred. Rungs 4 and 5 remain outside this claim.
